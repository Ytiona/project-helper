 const defaultConfig = {
  maxWidth: null,
  maxHeight: null,
  quality: 100,
  minSize: null,
  maxSize: 5
}

const COMPRESS_IMG_PREFIX = '[handled]-';

export {
  defaultConfig,
  COMPRESS_IMG_PREFIX
}